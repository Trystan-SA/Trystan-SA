# Postgres (used by docker-compose and the app)
POSTGRES_USER=trystan
POSTGRES_PASSWORD=change-me
POSTGRES_DB=trystan_sa
POSTGRES_PORT=5432

DATABASE_URL=postgres://trystan:change-me@localhost:5432/trystan_sa

# Giscus (public IDs from https://giscus.app — not secrets)
PUBLIC_GISCUS_REPO=XXX
PUBLIC_GISCUS_REPO_ID=XXX
PUBLIC_GISCUS_CATEGORY=XXX
PUBLIC_GISCUS_CATEGORY_ID=XXX
